# DeepSeek Harness 桌面客户端 · 版本迭代说明

面向使用者的发布说明。开发侧的技术细节见 `CHANGELOG.md`。

**如何升级**
- **v1.7.0 及以后**:客户端会自动检查新版本 → 弹通知 → 设置页点「下载更新」→「立即安装」,全程无需手动操作,数据与 API Key 保留。
- **v1.6.0 及更早**:自动更新能力是 v1.7.0 才加入的,需要**手动下载安装包覆盖安装一次**;之后即可享受自动推送。
- **手动安装**:运行 `releases\DeepSeekHarness-Setup-v{版本}-win10-x64.exe` 覆盖安装即可,无需卸载旧版,数据目录自动沿用。

---

## v1.7.0 — 自动更新(2026-09-11)

**一句话摘要**:客户端现在能自己检查和安装新版本了。

**新增**
- **自动更新**:启动 30 秒后检查一次,之后每 6 小时检查;发现新版本弹系统通知,点通知直接打开设置页
- **自己决定是否更新**:设置页显示新版本号与更新说明,可选「下载更新」或「忽略此版本」;忽略后不会反复打扰
- **下载进度与完整性校验**:下载显示百分比与 MB 进度;下载完成自动校验文件完整性,损坏自动重下
- **双下载源**:主源不可用时自动切换备用源,避免"检查到更新却下不动"
- **自定义更新源**:设置页可填写自己的更新源地址(企业内网部署用)

**改进**
- 更新安装为静默方式,完成后客户端自动重启,会话、API Key、设置全部保留
- 「检查更新」现在同时显示客户端版本与内置引擎版本两条信息

**修复**
- 修复静默安装可能改写「数据目录指向文件」的隐患:静默升级现在绝不会改动已配置的数据目录(数据安全加固)

---

## v1.6.0 — 稳定性与体积(2026-09-10)

**一句话摘要**:引擎崩了能自愈、睡眠唤醒能恢复、端口被占能自动换,并新增内置日志查看器。

**新增**
- **引擎崩溃自愈**:引擎意外退出后自动重启(2 秒起逐级退避,最多 5 次),重启后界面自动恢复;托盘与设置页都提供「重启引擎」手动入口
- **睡眠唤醒恢复**:合盖/休眠再唤醒后,若引擎失去响应会自动重新拉起,不再需要手动重开客户端
- **端口冲突自愈**:默认端口被别的程序占用时,自动改用其他可用端口并记住,不再出现"打不开界面"
- **内置日志查看器**:设置页可查看引擎日志与客户端日志(启动、崩溃、重启等事件),一键打包成 zip 便于反馈问题

**改进**
- 安装包体积从 145.1 MB 优化到 129.0 MB;安装文件数量减少约 1.3 万个,安装更快

---

## v1.5.4 — 首次配置修复

**一句话摘要**:修复每次启动都要求填写 API Key 的问题。

**修复**
- 修复客户端每次都弹出「首次配置」窗口、误判未配置 API Key 的问题(适配新版引擎的凭据文件格式)
- 修复保存 API Key 时可能覆盖同文件内其他授权记录的问题

**其他**
- 提供独立网页版启动器 `dsh webui`(桌面快捷方式),不想用客户端时可直接在浏览器里使用

---

## v1.5.2 / v1.5.1 — 安装过程修复

**一句话摘要**:修复静默安装可能卡住的问题。

**修复**
- 修复特定情况下静默安装长时间无响应的问题
- 修复静默安装结束时可能额外弹出客户端窗口的问题

---

## v1.5.0 — 引擎升级

**一句话摘要**:内置引擎升级到 0.1.2 系列,支持新版网页界面的安全访问机制。

**改进**
- 内置引擎升级到 `0.1.2-rc.1`,与网页版功能保持一致
- 适配新版引擎的一次性访问令牌机制,界面加载更可靠

---

## v1.3.0 — 引擎升级

**一句话摘要**:内置引擎升级到 0.1.1 系列,界面渲染架构更新。

**改进**
- 内置引擎升级到 `0.1.1-rc.2`
- 界面渲染切换到新架构,窗口缩放与布局更稳定

---

## v1.2.3 / v1.2.2 — 卸载数据安全(重要)

**一句话摘要**:卸载时「删除数据」绝不会删除你自己的文件。

**改进(三层保险)**
1. **按名清理**:只删除 DeepSeek Harness 自己的文件(会话、配置、凭据、日志等),目录里其他文件一律不动
2. **时间保护**:创建时间早于本程序安装时间的文件,一律保留
3. **非空保留**:数据目录只要还有其他文件,程序就不会删除该目录本身

**其他**
- 安装时若选择的是非空目录,会明确提示风险并要求确认
- 卸载窗口会告知「检测到 N 个早于本次安装的会话,将保留」

---

## v1.2.0 — 便携版冻结

**一句话摘要**:便携版定格在 v1.2.0,此后只维护安装版。

**说明**
- 便携版(v1.2.0)继续可用,但不再更新
- 后续所有新功能只进入安装版客户端

---

## v1.0.x — 客户端首发

**一句话摘要**:DeepSeek Harness 桌面客户端初版。

**核心功能**
- 自定义标题栏 + 日夜主题切换(界面、标题栏、内核同步)
- 系统托盘常驻:显示窗口、设置、创建快捷方式、退出;关窗最小化到托盘
- 单实例运行、窗口位置与大小记忆
- 首次运行引导填写 API Key
- 系统通知、数据备份与恢复、更新检查
- 全局快捷键 `Ctrl + Shift + D` 随时显示/隐藏
- 安装器:每用户安装(无需管理员)、自定义数据目录、桌面/开始菜单图标、自定义卸载确认

---

## 版本号规则

`主版本.功能版本.修复版本`,例如 `1.7.0`:
- 主版本:不兼容的大改动
- 功能版本:新增功能(v1.6.0 稳定性、v1.7.0 自动更新)
- 修复版本:只修问题,无新功能

内置引擎版本(如 `0.1.2-rc.1`)独立于客户端版本,可在设置页「检查更新」中查看。
